<?php
if ( ! function_exists( 'dbDelta' ) ) {
	/**
	 * Test stub for WordPress dbDelta.
	 *
	 * @param string $sql SQL statement.
	 * @return array<int, string>
	 */
	function dbDelta( $sql ) {
		unset( $sql );
		return array();
	}
}
